# Counter_Project
This is a basic counter application built using React.js as a practice project to understand core React concepts such as components, state, and event handling.

The app allows users to increment, decrement, and reset a counter value with instant UI updates.
